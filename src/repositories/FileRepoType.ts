export interface IFileRepo {
  // createFileMeta(targetList);
  // storageFileDeepCopy(fileId, workspaceId, channelId, userId);
  // createUploadMeta(dto);
  // createUploadStorage(fileId, file, onUploadProgress, workspaceId, channelId, userId);
  // uploadFileGW(
  //   file,
  //   fileName,
  //   fileExtension,
  //   location,
  //   onUploadProgress,
  //   cancelSource,
  //   channelId,
  //   pageId,
  // );
  // deleteFile(deleteFileId, channelId);
  // deleteAllFile(fileList, channelId);
  // getStorageVolume();
  // getDuplicateFile(fileName, fileExt, pageId);
  // getRecycleBinAllFile(channelId);
}
